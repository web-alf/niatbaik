node_modules/
# Build artifacts — generated by `bun run build` (build.mjs + tailwind).
# Produced fresh inside the Docker build stage; no need to commit.
# Anchored to repo root so src/public/ (source) stays tracked.
/public/
